# R project files
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Large data files
data/rolling_predictors.csv
data/zse-predictors*
data/*.csv
data/raw/
# ...but keep the small committed results
!data/results/preds_perf.csv

# Quarto cache and output
finml_results_cache/
finml_results_files/
paper/zse_ml_paper_files/
paper/zse_ml_paper_cache/
paper/finml_results_cache/
paper/finml_results_files/
paper/pre_live_cache/
paper/pre_live_files/
*_cache/
*_files/

# Rendered paper outputs in paper/ are working copies; the published versions
# live in docs/ (served via GitHub Pages). Keep sources (.qmd, .scss) tracked.
paper/zse_ml_paper.html
paper/zse_ml_paper.pdf
paper/forecasting_thin_markets.html
paper/forecasting_thin_markets.pdf
.quarto/

# Experiment registry (large, local only)
experiments_live/

# Secrets and credentials
blob_key.txt

# Commit history dumps (large, not needed in repo)
commits_*.txt
